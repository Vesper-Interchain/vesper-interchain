syntax = "proto3";
package vesperinterchain.oracle.v1;

option go_package = "github.com/Vesper-Interchain/vesper-interchain/x/oracle/types";

// Price defines the Price message.
message Price {
  string denom = 1;
  string price = 2;
  int64 timestamp = 3;
  string source = 4;
}
